import Navigation from 'components/Navigation';
import { Header } from './AppBar.styled';

const AppBar = () => {
  return (
    <Header>
      <Navigation />
    </Header>
  );
};

export default AppBar;